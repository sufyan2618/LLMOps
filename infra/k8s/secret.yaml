apiVersion: v1
kind: Secret
metadata:
  name: llmops-chat-secrets
  namespace: llmops
type: Opaque
stringData:
  # Replace via sealed-secrets / ExternalSecrets / CI — do not commit real keys
  LANGFUSE_PUBLIC_KEY: "pk-lf-CHANGE_ME"
  LANGFUSE_SECRET_KEY: "sk-lf-CHANGE_ME"
