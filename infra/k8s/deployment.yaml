apiVersion: apps/v1
kind: Deployment
metadata:
  name: llmops-chat
  namespace: llmops
  labels:
    app.kubernetes.io/name: llmops-chat
    app.kubernetes.io/component: api
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: llmops-chat
  template:
    metadata:
      labels:
        app.kubernetes.io/name: llmops-chat
        app.kubernetes.io/component: api
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8000"
        prometheus.io/path: "/metrics"
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        fsGroup: 10001
      containers:
        - name: api
          image: sufyanliaqat/llmops-chat:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8000
          envFrom:
            - configMapRef:
                name: llmops-chat-config
            - secretRef:
                name: llmops-chat-secrets
          volumeMounts:
            - name: models
              mountPath: /models
              readOnly: true
            - name: tmp
              mountPath: /tmp
          resources:
            requests:
              cpu: "500m"
              memory: "3Gi"
            limits:
              cpu: "3"
              memory: "5Gi"
          # Liveness must never call /ready (model load). Health is always cheap.
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 6
          # Ready only after background warm-up; long threshold so load can finish.
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 60
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
      volumes:
        - name: models
          persistentVolumeClaim:
            claimName: gguf-models-hostpath
            readOnly: true
        - name: tmp
          emptyDir: {}
