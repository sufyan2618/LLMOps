apiVersion: v1
kind: Service
metadata:
  name: llmops-chat-nodeport
  namespace: llmops
  labels:
    app.kubernetes.io/name: llmops-chat
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: llmops-chat
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30080
